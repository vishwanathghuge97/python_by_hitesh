

```python
squared_num = [x**2 for x in range(10)]
# → [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]
```

`range(10)` gives numbers 0 to 9. For each number `x`, it calculates `x**2` (x squared) and puts it in the list.

`cube_num = [y**3 for y in range(10)]` would give cubes the same way.